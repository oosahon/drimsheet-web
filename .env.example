# Public Vite values are embedded in the client bundle at build time.
VITE_API_URL=
VITE_LAUNCHDARKLY_KEY=
VITE_APP_ENV=local
VITE_SENTRY_DSN=

# Sentry source-map values are build-only. Never prefix the auth token with VITE_.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
